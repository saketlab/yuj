{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"yuj","text":"<p>yuj (Sanskrit: \"to yoke\", root of yoga): yoke borrowed machines to a long-running batch.</p> <p><code>yuj</code> scatters work across idle lab desktops over SSH and pulls results back, with a self-healing watchdog on each host. No daemons, no cluster admin, no root; it runs on SSH, cron, and rsync.</p>"},{"location":"#when-to-use-yuj","title":"When to use yuj","text":"You have\u2026 Use\u2026 Real HPC + cluster admin, can install daemons HTCondor Short jobs, controller stays up GNU Parallel (<code>--sshlogin</code>) Borrowed desktops, days-long batches, reboots + fail2ban yuj <p>yuj exists for the case where you can't install software on every node and can't run a central scheduler, but you do have SSH access and write access to <code>$HOME</code>.</p> <p>It only fits work you can split into independent items: each item runs on one host on its own, with no communication between items and no shared memory. If your job needs nodes to talk to each other mid-run (MPI, a shared address space), yuj is the wrong tool.</p>"},{"location":"#what-it-does","title":"What it does","text":"<pre><code>graph LR\n    A[Controller laptop] --&gt;|yuj deploy| B[Host 1]\n    A --&gt;|yuj deploy| C[Host 2]\n    A --&gt;|yuj deploy| D[Host N]\n    B --&gt;|watchdog + cron| B\n    C --&gt;|watchdog + cron| C\n    D --&gt;|watchdog + cron| D\n    B --&gt;|yuj pull| E[central results/]\n    C --&gt;|yuj pull| E\n    D --&gt;|yuj pull| E</code></pre> <ol> <li><code>yuj provision</code> (optional): if you only have an admin/sudo login, create a dedicated worker user with key auth and save its credentials</li> <li><code>yuj bootstrap</code>: install Python or R on each host (no root)</li> <li><code>yuj deploy</code>: rsync your code + data to <code>$HOME</code> on each host</li> <li><code>yuj submit</code>: start a watchdog + cron; hosts keep working even if your laptop reboots</li> <li><code>yuj status</code>: dashboard: producing \u00b7 idle \u00b7 stalled \u00b7 down</li> <li><code>yuj pull</code>: pull results back as they arrive</li> <li><code>yuj decommission</code>: remove the job and hand machines back clean</li> </ol>"},{"location":"#quick-start","title":"Quick start","text":"PythonRAny language <pre><code>pipx install yuj\nmkdir my-job &amp;&amp; cd my-job\nyuj init --template python              # scaffold worker.py + config\n$EDITOR fleet.csv                       # add your hosts\nyuj bootstrap                           # install Python (uv) on hosts\nyuj deploy &amp;&amp; yuj submit                # push code, start watchdog\nyuj status                              # see who's producing\nyuj pull --loop 60                      # gather results\n</code></pre> <pre><code>pipx install yuj\nmkdir my-r-job &amp;&amp; cd my-r-job\nyuj init --template r                   # scaffold worker.R + environment.yaml\n$EDITOR fleet.csv\nyuj bootstrap                           # install R via micromamba (no root)\nyuj deploy &amp;&amp; yuj submit\nyuj status --watch 30\nyuj pull --loop 60\n</code></pre> <pre><code>pipx install yuj\nmkdir my-job &amp;&amp; cd my-job\nyuj init --template bare                # minimal scaffold\n$EDITOR fleet.csv worker.sh             # your worker in any language\nyuj deploy &amp;&amp; yuj submit\nyuj status\nyuj pull\n</code></pre>"},{"location":"caveats/","title":"Caveats","text":""},{"location":"caveats/#use-ssh-keys-when-you-can","title":"Use SSH keys when you can","text":"<p>If the host owner can add your public key to <code>~/.ssh/authorized_keys</code>, do that instead of using a password. Keys don't trigger fail2ban on failed auths, work without <code>sshpass</code>, and aren't logged anywhere.</p> <p>To use a key, set <code>key_path</code> in <code>fleet.csv</code>:</p> <pre><code>username,ip,name,password,key_path\nalice,10.0.0.1,lab-desk-1,,/home/alice/.ssh/id_ed25519\n</code></pre>"},{"location":"caveats/#fail2ban-bans-stick","title":"fail2ban bans stick","text":"<p>Repeated failed authentications will get your controller's IP banned from a host for hours, sometimes longer. yuj limits the damage by:</p> <ul> <li>Defaulting <code>--max-workers 4</code> in bootstrap to keep concurrent probes low</li> <li>Distinguishing auth failures from other problems (<code>yuj diagnose</code>)</li> <li>Never retrying on an auth failure without you intervening</li> </ul> <p>Once you're banned, you can't un-ban yourself from outside. Wait it out or ask the host admin.</p>"},{"location":"caveats/#keep-fleetcsv-out-of-version-control","title":"Keep fleet.csv out of version control","text":"<p>Passwords go in there. yuj's <code>.gitignore</code> excludes <code>fleet.csv</code> and <code>*.creds</code> by default. Check before pushing, and don't paste <code>fleet.csv</code> contents into tickets or Slack.</p> <p>Passwords are passed to <code>sshpass</code> via the <code>SSHPASS</code> environment variable, never on the command line (where they'd show up in <code>ps</code> output).</p>"},{"location":"caveats/#these-are-someone-elses-machines","title":"These are someone else's machines","text":"<p><code>yuj status</code> shows <code>\u26a0</code> when someone is at the console. When that happens, stop the job or schedule a decommission:</p> <pre><code>yuj decommission lab-desk-1                         # now\nyuj decommission lab-desk-1 --at \"8am tomorrow\"     # polite\n</code></pre>"},{"location":"caveats/#stall-detection-needs-tuning","title":"Stall detection needs tuning","text":"<p>The default <code>stall_min: 90</code> (90 minutes) suits long-running jobs that load big models. For faster workloads, lower it:</p> <pre><code>stall_min: 5   # restart if no new output for 5 minutes\n</code></pre> <p>But make sure <code>grace_sec</code> is long enough to cover your startup time (default 2700 s = 45 min). Without sufficient grace, the watchdog will restart a warming-up job.</p>"},{"location":"caveats/#yuj-is-not-a-scheduler","title":"yuj is not a scheduler","text":"<p>yuj doesn't do job queuing, resource limits, inter-job dependencies, or GPU isolation. It keeps a batch making progress across a set of unreliable machines. For queuing, resource limits, or GPU isolation, use Slurm or Kubernetes.</p>"},{"location":"caveats/#no-host-key-verification-by-default","title":"No host key verification by default","text":"<p>yuj uses <code>StrictHostKeyChecking=no</code> and <code>UserKnownHostsFile=/dev/null</code> because borrowed lab desktops get reimaged frequently and their host keys change. This is fine for a trusted lab subnet. If you're connecting over the public internet, configure proper <code>known_hosts</code> and set <code>StrictHostKeyChecking=yes</code> in your SSH config.</p>"},{"location":"cli/","title":"CLI reference","text":"<p>All commands read settings from <code>yuj.yaml</code> in the current directory; flags override. Run any command with <code>--help</code> for the full option list.</p>"},{"location":"cli/#yuj-init","title":"<code>yuj init</code>","text":"<p>Scaffold a ready-to-edit project. Never clobbers existing files.</p> <pre><code>yuj init [DIRECTORY] [--template {bare,python,r}]\n</code></pre> Template Creates <code>bare</code> <code>fleet.csv</code>, <code>yuj.yaml</code>, <code>worker.sh</code> <code>python</code> + <code>worker.py</code>, <code>items.txt</code> <code>r</code> + <code>worker.R</code>, <code>items.txt</code>, <code>environment.yaml</code>, <code>r-packages.txt</code> <pre><code>yuj init my-job --template r\n</code></pre>"},{"location":"cli/#yuj-provision","title":"<code>yuj provision</code>","text":"<p>Create an unprivileged worker user on every host and save its credentials. This is the one step that needs privilege: <code>--fleet</code> here is an admin fleet whose accounts can <code>sudo</code>.</p> <pre><code>yuj provision [--fleet ADMIN_CSV] [--hosts a,b,...] [--user yuj]\n              [--ask-sudo-pass] [--key-dir .yuj/keys] [--out provisioned-fleet.csv]\n              [--check] [--max-workers 4]\n</code></pre> <p>yuj generates one ed25519 keypair, installs the public key in each new user's <code>~/.ssh/authorized_keys</code> (key auth only, no password is set), locks the account's password, and writes a ready-to-use worker fleet CSV.</p> Flag Description <code>--user</code> Worker username to create (default <code>yuj</code>) <code>--ask-sudo-pass</code> Prompt for the admin's sudo password; by default the admin's SSH password is reused <code>--key-dir</code> Where the generated private key is stored (default <code>.yuj/keys/</code>) <code>--out</code> Path for the generated worker fleet CSV (default <code>provisioned-fleet.csv</code>) <code>--check</code> Dry-run: report what would happen, create nothing <p>The sudo password travels over SSH stdin (<code>sudo -S</code>), never on a command line. Re-running is idempotent: the keypair is reused, existing users are left in place, and the public key is appended only if missing. Feed the result straight into bootstrap:</p> <pre><code>yuj provision --fleet admin.csv --user yuj\nyuj bootstrap --fleet provisioned-fleet.csv\n</code></pre>"},{"location":"cli/#yuj-bootstrap","title":"<code>yuj bootstrap</code>","text":"<p>Install an environment manager + extras on every host (idempotent, no root).</p> <pre><code>yuj bootstrap [--fleet PATH] [--hosts a,b,...] [--env-manager uv|pixi|micromamba|conda]\n              [--python 3.12] [--extras OLLAMA,R] [--check] [--max-workers 4]\n</code></pre> Flag Description <code>--env-manager</code> Environment manager to install <code>--extras OLLAMA,R</code> Named bundles to install (R, OLLAMA, SHELLCHECK, RCLONE) <code>--check</code> Dry-run: print what would be done, install nothing <code>--max-workers</code> Max parallel hosts (default 4; keep low to avoid fail2ban)"},{"location":"cli/#yuj-deploy","title":"<code>yuj deploy</code>","text":"<p>Rsync code + payload to every host.</p> <pre><code>yuj deploy [--fleet PATH] [--hosts a,b,...] [--no-payload]\n</code></pre> <p><code>--no-payload</code> skips heavy shared data (e.g. a multi-GB cache) and sends only the code, the lightweight redistribution path for re-runs.</p>"},{"location":"cli/#yuj-submit","title":"<code>yuj submit</code>","text":"<p>Install the self-healing watchdog + cron on every host and start the job.</p> <pre><code>yuj submit [--fleet PATH] [--hosts a,b,...] [--no-start]\n</code></pre> <p><code>--no-start</code> installs the scripts + cron but doesn't launch the watchdog. Use it for staging.</p>"},{"location":"cli/#yuj-status","title":"<code>yuj status</code>","text":"<p>Fleet-wide dashboard.</p> <pre><code>yuj status [--fleet PATH] [--results-glob GLOB] [--stall-min N] [--watch N]\n</code></pre> Column Meaning state <code>\u25cf producing</code> \u00b7 <code>\u25cf stalled</code> \u00b7 <code>\u25cb idle</code> \u00b7 <code>\u25cf down</code> cpu Core count + model gpu GPU name + memory mem RAM in GB load 1-min load average (green &lt; 0.7/core, yellow &lt; 1.2, red \u2265 1.2) outputs Count of result files matching <code>results_glob</code> age Minutes since newest output (stall detection) owner <code>\u26a0 alice</code> if a human is at the console <p><code>--watch 30</code> enters live mode, refreshing every 30 seconds. Press Ctrl-C to exit.</p>"},{"location":"cli/#yuj-fleet-probe","title":"<code>yuj fleet probe</code>","text":"<p>Same as <code>yuj status</code> but always one-shot (no live mode).</p> <pre><code>yuj fleet probe [--fleet PATH] [--results-glob GLOB]\n</code></pre>"},{"location":"cli/#yuj-pull","title":"<code>yuj pull</code>","text":"<p>Rsync outputs from the fleet back to a central directory.</p> <pre><code>yuj pull [--once | --loop N] [--fleet PATH] [--dest DIR]\n</code></pre> <p><code>--loop 60</code> runs every 60 seconds (press Ctrl-C to stop).</p>"},{"location":"cli/#yuj-diagnose","title":"<code>yuj diagnose</code>","text":"<p>Classify why hosts are (un)reachable, with fail2ban awareness.</p> <pre><code>yuj diagnose [--fleet PATH] [--hosts a,b,...]\n</code></pre> Status Meaning <code>\u2713 ok</code> Auth succeeded <code>auth refused</code> Bad password/key or fail2ban ban <code>banner fail</code> TCP connects, SSH drops (classic fail2ban) <code>sshd down</code> Port 22 closed / refused <code>net down</code> No route / timeout"},{"location":"cli/#yuj-decommission","title":"<code>yuj decommission</code>","text":"<p>Remove the yuj job from a host, now or scheduled.</p> <pre><code>yuj decommission HOST [--fleet PATH] [--at \"WHEN\"] [--remove-dir]\n</code></pre> <p><code>--at</code> accepts relative delays (<code>+90 seconds</code>, <code>+2 hours</code>) or absolute times (<code>\"9am tomorrow\"</code>, <code>\"17:00\"</code>) via the host's <code>at</code> command.</p> <p><code>--remove-dir</code> also deletes the deploy directory (<code>~/yuj-run</code> by default).</p> <p>Job-scoped teardown</p> <p>Decommission removes only yuj's cron entry and processes, identified by the job name. Any production job running on the same host is untouched.</p>"},{"location":"cli/#yuj-stop-host","title":"<code>yuj stop HOST</code>","text":"<p>Touch the stop sentinel (<code>/tmp/&lt;job&gt;.yuj.stop</code>) for a graceful shutdown. The watchdog exits on its next check interval.</p>"},{"location":"cli/#global-options","title":"Global options","text":"Option Description <code>--fleet PATH</code> Override the fleet file (default: <code>fleet.csv</code> or <code>yuj.yaml:fleet</code>) <code>--hosts a,b</code> Run only on named hosts; refuses <code>do_not_use</code> hosts <code>--help</code> Show help for any command"},{"location":"concepts/","title":"Concepts","text":""},{"location":"concepts/#the-run-model","title":"The run model","text":"<p>A batch is a list of string items: accessions, sample IDs, URLs, filenames, anything. yuj splits the list across hosts, runs your command once per item, and checks completion by looking for the output file, never a job log.</p> <pre><code>batch = [item1, item2, \u2026, itemN]\n\nper host:\n  for item in host_items:\n      if output_file_exists(item): skip\n      run: work_command  item\n</code></pre> <p>This works for shell scripts, Python, R, Julia, or any compiled binary callable from a command line.</p> <p>yuj only fits embarrassingly parallel work</p> <p>Each item runs on its own, on one host, with no communication between items and no shared memory. yuj never moves data between hosts mid-run. If your work needs nodes to exchange state while it runs (MPI, a shared address space, a distributed reduction, or a job where item B depends on item A's in-memory result), yuj is the wrong tool. Split the batch into independent items, or use a real cluster scheduler.</p>"},{"location":"concepts/#resume-by-output-file","title":"Resume by output file","text":"<p>When a worker crashes mid-write, the only reliable way to know an item is done is to check whether the output file exists. yuj uses this exclusively, not a job log or database. In practice:</p> <ul> <li>A worker that crashes halfway through writing an item leaves no output file and gets retried on the next watchdog restart.</li> <li>Two hosts can process the same item; whichever finishes last wins. Safe because <code>yuj pull</code> merges into a single directory.</li> <li>Changing <code>output_suffix</code> changes what yuj considers done. Don't change it mid-run.</li> </ul>"},{"location":"concepts/#the-watchdog-chain","title":"The watchdog chain","text":"<pre><code>cron (every 15 min)\n  \u2514\u2500 ensure.sh   \u2192 is the watchdog running?\n                      yes \u2192 exit 0\n                      no  \u2192 launch watchdog (setsid + &amp;)\n\nwatchdog.sh  (runs continuously)\n  \u251c\u2500 every INTERVAL seconds: is the run-loop alive?\n  \u2502     dead \u2192 relaunch\n  \u2502     alive but newest output &gt; STALL_MIN min old \u2192 kill + relaunch\n  \u2502     alive, recent output \u2192 log \"ok\"\n  \u2514\u2500 exits when stop sentinel exists\n\nrun-loop.sh\n  \u2514\u2500 for item in items.txt:\n         if output exists: skip\n         work_command item\n</code></pre> <p>Because supervision lives in cron on the remote host, a controller reboot doesn't touch running jobs. Hosts keep going. The controller reconnects with <code>yuj pull</code> and picks up where results left off.</p>"},{"location":"concepts/#isolation-from-production","title":"Isolation from production","text":"<p>When multiple jobs run on the same host, yuj uses the job name to make every artifact unique:</p> Artifact Pattern Scripts <code>~/&lt;remote_dir&gt;/&lt;job&gt;.yuj-run.sh</code>, <code>&lt;job&gt;.yuj-watchdog.sh</code>, <code>&lt;job&gt;.yuj-ensure.sh</code> Cron entry contains <code>&lt;job&gt;.yuj-ensure.sh</code> Stop sentinel <code>/tmp/&lt;job&gt;.yuj.stop</code> Results controlled by <code>results_glob</code> in <code>yuj.yaml</code> <p><code>yuj decommission</code> kills processes and removes the cron entry by matching the job-unique script names. It cannot touch a production job running beside it.</p>"},{"location":"concepts/#weighted-distribution","title":"Weighted distribution","text":"<p>Hosts differ in capacity. yuj uses the largest-remainder (Hamilton) method to split items proportionally to per-host weights, so item counts sum exactly to the batch size with none lost.</p> <pre><code># fleet.csv\nusername,ip,name,password,weight\nalice,10.0.0.1,gpu-rig,p,4     # 4\u00d7 weight \u2192 gets 4\u00d7 more items\nalice,10.0.0.2,laptop,p,1\n</code></pre>"},{"location":"concepts/#do_not_use","title":"do_not_use","text":"<p>Mark decommissioned or off-limits hosts with <code>do_not_use: true</code>:</p> <pre><code>username,ip,name,password,do_not_use\nalice,10.0.0.3,old-box,p,true\n</code></pre> <ul> <li><code>yuj deploy</code> (default: all hosts) silently skips them.</li> <li><code>yuj deploy --hosts old-box</code> (explicit) refuses with a non-zero exit.</li> </ul>"},{"location":"concepts/#provisioning-the-one-privileged-step","title":"Provisioning (the one privileged step)","text":"<p>Everything else in yuj runs as an ordinary user, but creating that user needs root. <code>yuj provision</code> is the optional first step for when you have an admin login that can <code>sudo</code> but no dedicated worker account yet:</p> <pre><code>yuj provision --fleet admin.csv --user yuj\n</code></pre> <p>It connects as the admin, and via <code>sudo</code>:</p> <ol> <li>creates the worker user (<code>useradd -m</code>) if absent,</li> <li>installs a yuj-generated SSH public key in the user's <code>authorized_keys</code>,</li> <li>locks the account's password, so only the key works.</li> </ol> <p>One ed25519 keypair is generated on the controller for the whole fleet; the private half stays under <code>.yuj/keys/</code> (gitignored) and a <code>provisioned-fleet.csv</code> points each host's <code>key_path</code> at it. The admin's sudo password rides SSH stdin (<code>sudo -S</code>), never a command line. Because the new account is key-only, it never trips fail2ban, the same reason the rest of yuj prefers keys.</p>"},{"location":"concepts/#politeness","title":"Politeness","text":"<p>yuj runs on machines that belong to other people:</p> <ul> <li><code>yuj status</code> shows an owner-present warning when someone is logged into the console (detected via <code>who</code>).</li> <li><code>yuj decommission --at \"9am tomorrow\"</code> schedules a teardown so you hand machines back at a set time.</li> <li><code>--max-workers 4</code> in bootstrap limits concurrent SSH probes to avoid tripping subnet-level IDS rules.</li> </ul>"},{"location":"install/","title":"Installation","text":"<p>Install yuj on your laptop (the controller). You don't need anything on the remote hosts; that is what <code>yuj bootstrap</code> is for.</p>"},{"location":"install/#recommended-pipx","title":"Recommended: pipx","text":"<p>pipx installs yuj into its own environment and puts <code>yuj</code> on your <code>$PATH</code>.</p> <pre><code>pipx install yuj\nyuj version\n</code></pre>"},{"location":"install/#uv-tool","title":"uv tool","text":"<pre><code>uv tool install yuj\nyuj version\n</code></pre>"},{"location":"install/#pip-inside-a-venv","title":"pip (inside a venv)","text":"<pre><code>python -m venv .venv &amp;&amp; source .venv/bin/activate\npip install yuj\n</code></pre>"},{"location":"install/#requirements","title":"Requirements","text":"<ul> <li>Python \u2265 3.12 on your laptop</li> <li><code>ssh</code> and <code>rsync</code> on your laptop (standard on macOS and Linux)</li> <li> <p><code>sshpass</code>, only needed for password-based hosts</p> Ubuntu / DebianmacOS (Homebrew)Fedora / RHEL <pre><code>sudo apt install sshpass\n</code></pre> <pre><code>brew install hudochenkov/sshpass/sshpass\n</code></pre> <pre><code>sudo dnf install sshpass\n</code></pre> </li> </ul> <p>SSH keys avoid fail2ban</p> <p>If the host owner can add your public key to <code>~/.ssh/authorized_keys</code>, use <code>key_path</code> in <code>fleet.csv</code>. Keys never trigger fail2ban and don't need <code>sshpass</code>.</p>"},{"location":"install/#verify","title":"Verify","text":"<pre><code>$ yuj version\n0.1.0.dev0\n\n$ yuj --help\n Usage: yuj [OPTIONS] COMMAND [ARGS]...\n ...\n</code></pre>"},{"location":"quickstart/","title":"Quickstart","text":"<p>This page walks through a complete Python job. For R, see R users.</p>"},{"location":"quickstart/#1-scaffold-a-project","title":"1. Scaffold a project","text":"<pre><code>mkdir my-batch &amp;&amp; cd my-batch\nyuj init --template python\n</code></pre> <p>Creates:</p> <pre><code>my-batch/\n  fleet.csv       \u2190 add your hosts here\n  yuj.yaml        \u2190 job config (pre-filled)\n  worker.py       \u2190 your analysis code\n  items.txt       \u2190 one item per line\n</code></pre>"},{"location":"quickstart/#2-add-your-hosts","title":"2. Add your hosts","text":"<p>Edit <code>fleet.csv</code>:</p> <pre><code>username,ip,name,password\nalice,10.0.0.1,lab-desk-1,s3cret\nalice,10.0.0.2,lab-desk-2,\n</code></pre> <p>Key-based auth</p> <p>Leave <code>password</code> empty and add <code>key_path=/home/alice/.ssh/id_ed25519</code> to avoid fail2ban risk and skip <code>sshpass</code>.</p>"},{"location":"quickstart/#3-write-your-worker","title":"3. Write your worker","text":"<p><code>worker.py</code> already has the right structure. Replace the placeholder:</p> <pre><code>def main() -&gt; None:\n    item = sys.argv[1]                              # provided by yuj\n    out_dir = Path(os.environ.get(\"YUJ_OUT\", \"results\"))\n    out_dir.mkdir(parents=True, exist_ok=True)\n\n    result = analyze(item)\n    (out_dir / f\"{item}.out\").write_text(result)\n</code></pre> <p>The contract:</p> Rule Why Item is the last CLI argument yuj's run-loop appends it Output goes to <code>$YUJ_OUT/&lt;item&gt;&lt;suffix&gt;</code> yuj's resume check looks here Catch exceptions per-item one bad item shouldn't abort the batch"},{"location":"quickstart/#4-bootstrap","title":"4. Bootstrap","text":"<p>Install Python (via <code>uv</code>) on every host:</p> <pre><code>yuj bootstrap\n</code></pre> <pre><code> yuj bootstrap\n\u250f\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2513\n\u2503 host        \u2503 result   \u2503 detail                              \u2503\n\u2521\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2529\n\u2502 lab-desk-1  \u2502 \u2713 ok     \u2502 bootstrapped [Ubuntu 22.04.5 LTS]  \u2502\n\u2502 lab-desk-2  \u2502 \u2713 ok     \u2502 bootstrapped [Ubuntu 24.04.1 LTS]  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>Re-running bootstrap on an already-configured host is a no-op.</p>"},{"location":"quickstart/#5-deploy","title":"5. Deploy","text":"<pre><code>yuj deploy\n</code></pre>"},{"location":"quickstart/#6-submit","title":"6. Submit","text":"<pre><code>yuj submit\n</code></pre> <p>Installs the watchdog and cron on each host and starts the job. The watchdog relaunches your worker if it dies or stalls. A cron entry restarts the watchdog after a reboot, with no controller involvement needed.</p>"},{"location":"quickstart/#7-watch","title":"7. Watch","text":"<pre><code>yuj status --watch 30       # refresh every 30 seconds (Ctrl-C to exit)\n</code></pre> <pre><code>                     yuj fleet \u00b7 mypyjob\n\u250f\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2533\u2501\u2501\u2501\u2501\u2501\u2513\n\u2503 host        \u2503 ip       \u2503 state       \u2503 cpu           \u2503 outputs \u2503 age \u2503\n\u2521\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2547\u2501\u2501\u2501\u2501\u2501\u2529\n\u2502 lab-desk-1  \u2502 10.0.0.1 \u2502 \u25cf producing \u2502 8c \u00b7 Xeon \u2026  \u2502      42 \u2502  2m \u2502\n\u2502 lab-desk-2  \u2502 10.0.0.2 \u2502 \u25cb idle      \u2502 16c \u00b7 EPYC \u2026 \u2502       0 \u2502   - \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2518\n2/2 up \u00b7 1 producing \u00b7 0 stalled \u00b7 42 outputs \u00b7 0 with owner present\n</code></pre>"},{"location":"quickstart/#8-pull-results","title":"8. Pull results","text":"<pre><code>yuj pull --loop 60          # pull every 60 seconds, Ctrl-C to stop\nyuj pull                    # or just once\n</code></pre> <p>Results land in <code>central/</code> by default.</p>"},{"location":"quickstart/#9-decommission","title":"9. Decommission","text":"<pre><code>yuj decommission lab-desk-1                         # now\nyuj decommission lab-desk-2 --at \"9am tomorrow\"     # scheduled\n</code></pre> <p>Removes only yuj's cron entry and processes; any production job on the same host is untouched.</p>"},{"location":"r-users/","title":"R users","text":"<p>You don't need root access, a pre-installed R, or a cluster admin. yuj bootstraps R via micromamba (a small conda implementation) and installs your packages from conda-forge and CRAN.</p>"},{"location":"r-users/#how-it-works","title":"How it works","text":"<pre><code>laptop (yuj init --template r)\n  \u2502\n  \u251c\u2500\u2500 yuj bootstrap \u2500\u2500\u2192 installs micromamba on each host (no root)\n  \u2502                      micromamba creates the conda env (r-base, r-data.table, \u2026)\n  \u2502                      R extra installs CRAN packages into ~/.yuj-rlib\n  \u2502\n  \u251c\u2500\u2500 yuj deploy    \u2500\u2500\u2192 copies worker.R, items.txt, r-packages.txt\n  \u2502\n  \u251c\u2500\u2500 yuj submit    \u2500\u2500\u2192 watchdog calls  Rscript worker.R &lt;item&gt;  per item\n  \u2502                      skips items whose .csv already exists\n  \u2502\n  \u2514\u2500\u2500 yuj pull      \u2500\u2500\u2192 gathers all *.csv back to central/\n</code></pre>"},{"location":"r-users/#getting-started","title":"Getting started","text":"<pre><code>yuj init --template r\n$EDITOR fleet.csv           # add host IPs + passwords (or key paths)\nyuj bootstrap               # install R on every host (~5 min first time, no-op after)\nyuj deploy &amp;&amp; yuj submit\nyuj status --watch 30\nyuj pull --loop 60\n</code></pre> <p>No SSHing in, no <code>conda activate</code>, no sudo.</p>"},{"location":"r-users/#what-gets-created","title":"What gets created","text":"<p><code>yuj init --template r</code> creates six files:</p> File Edit <code>fleet.csv</code> Your borrowed hosts <code>yuj.yaml</code> Usually fine as-is <code>worker.R</code> Replace the analysis block <code>items.txt</code> Replace with your input items <code>environment.yaml</code> Add conda-forge R packages <code>r-packages.txt</code> Add CRAN-only packages"},{"location":"r-users/#writing-your-worker","title":"Writing your worker","text":"<p>The item is the last command-line argument:</p> <pre><code>args &lt;- commandArgs(trailingOnly = TRUE)\nitem &lt;- args[[1]]\n</code></pre> <p>Write output to <code>$YUJ_OUT/&lt;item&gt;.csv</code> (yuj sets <code>$YUJ_OUT</code>):</p> <pre><code>out_dir &lt;- Sys.getenv(\"YUJ_OUT\", \"results\")\ndir.create(out_dir, showWarnings = FALSE, recursive = TRUE)\nout &lt;- file.path(out_dir, paste0(item, \".csv\"))\nfwrite(result_dt, out)\n</code></pre> <p>Catch errors per-item so one failure doesn't abort the whole batch:</p> <pre><code>result &lt;- tryCatch({\n  data.table(item = item, value = compute(item))\n}, error = function(e) {\n  data.table(item = item, value = NA, error = conditionMessage(e))\n})\nfwrite(result, out)\n</code></pre>"},{"location":"r-users/#installing-packages","title":"Installing packages","text":""},{"location":"r-users/#conda-forge-preferred","title":"conda-forge (preferred)","text":"<p>Add packages to <code>environment.yaml</code>. conda-forge has pre-built binaries for most popular R packages, so there's no compiler or source build:</p> <pre><code>dependencies:\n  - r-base&gt;=4.3\n  - r-tidyverse\n  - r-biocmanager\n  - r-seurat\n  - bioconductor-deseq2\n</code></pre> <p>Then <code>yuj bootstrap</code> to pick up new packages (only installs what's new).</p>"},{"location":"r-users/#cran-via-r-packagestxt","title":"CRAN via r-packages.txt","text":"<p>For packages not on conda-forge:</p> <pre><code>scMerge\ntradeSeq\n</code></pre> <p>yuj's R extra installs these into <code>~/.yuj-rlib</code> on each host. Your worker already loads this library:</p> <pre><code>.libPaths(c(Sys.getenv(\"R_LIBS_USER\", \"~/.yuj-rlib\"), .libPaths()))\n</code></pre>"},{"location":"r-users/#a-real-example-seurat-clustering","title":"A real example: Seurat clustering","text":"<p><code>environment.yaml</code>: <pre><code>name: yuj-r\nchannels: [conda-forge]\ndependencies:\n  - r-base&gt;=4.3\n  - r-data.table\n  - r-seurat\n  - r-hdf5r\n</code></pre></p> <p><code>worker.R</code>: <pre><code>.libPaths(c(Sys.getenv(\"R_LIBS_USER\", \"~/.yuj-rlib\"), .libPaths()))\nsuppressMessages({\n  library(Seurat)\n  library(data.table)\n})\n\nargs    &lt;- commandArgs(trailingOnly = TRUE)\nsample  &lt;- args[[1]]\nout_dir &lt;- Sys.getenv(\"YUJ_OUT\", \"results\")\ndir.create(out_dir, showWarnings = FALSE, recursive = TRUE)\n\nresult &lt;- tryCatch({\n  mat &lt;- Read10X_h5(file.path(\"data\", paste0(sample, \".h5\")))\n  seu &lt;- CreateSeuratObject(mat, project = sample)\n  seu &lt;- NormalizeData(seu) |&gt; FindVariableFeatures() |&gt;\n         ScaleData() |&gt; RunPCA() |&gt; FindNeighbors() |&gt; FindClusters()\n  data.table(sample = sample, n_cells = ncol(seu),\n             n_clusters = max(seu$seurat_clusters) + 1)\n}, error = function(e) {\n  data.table(sample = sample, n_cells = NA, error = conditionMessage(e))\n})\n\nfwrite(result, file.path(out_dir, paste0(sample, \".csv\")))\n</code></pre></p> <p>To push the h5 files as payload: <pre><code># yuj.yaml\ndeploy:\n  code: [worker.R, items.txt, r-packages.txt]\n  payload: [data/]          # rsync the h5 files to each host\n</code></pre></p> <pre><code>yuj bootstrap\nyuj deploy       # transfers data/ + worker.R to every host\nyuj submit\nyuj pull\n</code></pre>"},{"location":"r-users/#combining-results-on-the-controller","title":"Combining results on the controller","text":"<pre><code>library(data.table)\nresults &lt;- rbindlist(\n  lapply(list.files(\"central\", \"\\\\.csv$\", full.names = TRUE), fread),\n  fill = TRUE          # handle error rows with missing columns\n)\nprint(results)\n</code></pre>"},{"location":"r-users/#troubleshooting","title":"Troubleshooting","text":"<p>\"Rscript not found\" : R wasn't bootstrapped on that host, or the <code>env.sh</code> PATH is wrong. Re-run <code>yuj bootstrap</code> and check <code>yuj status</code>.</p> <p>\"there is no package called 'X'\" : Add <code>r-X</code> to <code>environment.yaml</code> (preferred) or <code>X</code> to <code>r-packages.txt</code>, then <code>yuj bootstrap</code> again.</p> <p>\"cannot allocate vector of size N\" : The item needs more RAM than available. Reduce chunk size or mark that host <code>do_not_use: true</code> in <code>fleet.csv</code>.</p> <p>Host shows <code>\u25cf stalled</code> : The watchdog is up but output has gone stale. yuj will restart the run-loop after <code>stall_min</code> minutes. Run <code>yuj diagnose</code> to see what's happening on the host.</p>"},{"location":"testing/","title":"Testing your setup","text":"<p>There are two levels of tests: local unit tests (run in seconds, no SSH needed) and real-fleet e2e tests (opt-in, touch real hosts).</p>"},{"location":"testing/#running-the-unit-tests","title":"Running the unit tests","text":"<pre><code># Install dev dependencies (includes pytest, shellcheck-py, etc.)\nuv sync\n\n# Run all unit tests\nuv run pytest\n\n# With coverage\nuv run pytest --cov=yuj --cov-report=term-missing\n</code></pre> <p>Expected output:</p> <pre><code>262 passed, 5 skipped in 2.0s\n</code></pre> <p>The 5 skipped are the real-fleet e2e tests; they don't run unless you opt in (see below).</p>"},{"location":"testing/#verifying-your-own-installation","title":"Verifying your own installation","text":"<pre><code># 1. Check the CLI works\nyuj version\nyuj --help\n\n# 2. Scaffold a project and inspect it\nyuj init --template r my-test-job\nls my-test-job\n\n# 3. Dry-run bootstrap (no changes made)\ncd my-test-job &amp;&amp; yuj bootstrap --check\n</code></pre> <p><code>yuj bootstrap --check</code> prints what it would install on each host without running anything. Use it to verify the fleet CSV before committing.</p>"},{"location":"testing/#probing-your-fleet","title":"Probing your fleet","text":"<pre><code>yuj diagnose\n</code></pre> <p>Classifies each host:</p> Status Meaning <code>\u2713 ok</code> SSH works, auth succeeded <code>auth refused</code> Wrong password/key, or fail2ban banned your IP <code>banner fail (fail2ban?)</code> TCP connects but SSH banner dropped (classic fail2ban) <code>sshd down</code> Port 22 closed / connection refused <code>net down</code> No route, timeout <pre><code>yuj status            # full dashboard (CPU, GPU, load, outputs, age, owner)\n</code></pre>"},{"location":"testing/#real-fleet-e2e-tests","title":"Real-fleet e2e tests","text":"<p>These tests SSH into real hosts</p> <p>The e2e suite is fully opt-in. Run it only on machines you own or have explicit permission to use. Do not run on a Friday evening if a production job is running alongside.</p>"},{"location":"testing/#safety-isolation","title":"Safety isolation","text":"<p>Every e2e test uses <code>job=\"yuj-test\"</code>, which makes all artifacts unique:</p> Artifact production yuj e2e Deploy dir <code>~/hpc_v3/</code> <code>~/yuj-test/</code> Cron line <code>\u2026ensure_watchdog.sh</code> <code>\u2026yuj-test.yuj-ensure.sh</code> Sentinel <code>/tmp/watchdog.stop</code> <code>/tmp/yuj-test.yuj.stop</code> Results <code>hpc_v3/results/</code> <code>~/yuj-test/results/</code> <p>A session finalizer decommissions every touched host at the end, and the suite fails if any production <code>run_b20.sh</code> process count drops.</p>"},{"location":"testing/#running","title":"Running","text":"<pre><code>uv run pytest tests/e2e/ \\\n  --fleet=/path/to/fleet.csv \\\n  --launchpad-cred=~/.my-creds \\\n  --include-hosts=myhost1,myhost2 \\\n  --tb=short -v\n</code></pre> <p>Options:</p> Option Description <code>--fleet PATH</code> Fleet CSV with hosts + credentials <code>--include-hosts HOST,...</code> Required opt-in list (never runs on all by default) <code>--launchpad-cred PATH</code> File with <code>kcdh_password=...</code> for launchpad <code>--keep</code> Don't delete <code>~/yuj-test/</code> after the run"},{"location":"testing/#scenarios","title":"Scenarios","text":"# Test What it proves 1 <code>test_bootstrap_real.py</code> Bootstrap (uv + Ollama), idempotency 2 <code>test_deploy_and_pull.py</code> 200-item job completes; all digests correct 6 <code>test_probe_classifies.py</code> Diagnose net/sshd/banner/auth 7 <code>test_do_not_use_respected.py</code> <code>do_not_use</code> flag enforced 8 Cleanup assertion in session finalizer No stray cron/procs/dirs"},{"location":"testing/#template-shellcheck","title":"Template shellcheck","text":"<p>The generated on-host scripts are shellcheck-clean:</p> <pre><code>uv run pytest tests/test_templates.py tests/test_bootstrap.py -v\n</code></pre>"},{"location":"testing/#ci","title":"CI","text":"<p>The GitHub Actions workflow runs on every push:</p> <pre><code># .github/workflows/ci.yml\n- Lint (ruff)\n- Format check (ruff format)\n- Type check (mypy --strict)\n- Tests (pytest, matrix: Python 3.12 + 3.13)\n</code></pre> <p></p>"}]}